%section.p-section-card
  .p-section-card__header.p-content-header
    %h2.p-content-header__title #{@place.name} の訪問記録を修正

  = form_with model: @report, url: visited_place_report_path(@place.id, @report.id), method: :put, local: true, class: 'p-form p-section-card__content' do |f|
    = render 'form_contents', f: f,  place: @place
